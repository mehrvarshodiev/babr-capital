import { Award, Eye, Shield, UsersRound } from 'lucide-react'; import type { Copy } from '../../data/translations'; export default function Values({t}:{t:Copy}){const icons=[Shield,UsersRound,Eye,Award];return <section id="values" className="section bg-slate-50/70 dark:bg-white/[.015]"><div className="mx-auto max-w-7xl px-6"><span className="section-kicker">{t.values.kicker}</span><h2 className="section-title mt-3 max-w-2xl">{t.values.title}</h2><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{t.values.items.map(([title,text],i)=>{const Icon=icons[i];return <article className="card p-6" key={title}><Icon className="text-[#c8923e]" size={25}/><h3 className="mt-5 font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500 dark:text-slate-400">{text}</p></article>})}</div></div></section>}
