import { Award, Eye, Shield, UsersRound } from 'lucide-react'; import type { Copy } from '../../data/translations'; export default function Values({t}:{t:Copy}){const icons=[Shield,UsersRound,Eye,Award];return <section id="values" className="section-pad"><div className="mx-auto max-w-7xl"><div className="section-heading"><span className="section-kicker">03</span><div><p className="kicker">{t.values.kicker}</p><h2>{t.values.title}</h2></div></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{t.values.items.map(([title,text],i)=>{const Icon=icons[i];return <article className="glass-hover glass value-card rounded-[2rem]" key={title}><span>0{i+1}</span><div className="value-icon"><Icon size={20}/></div><h3>{title}</h3><p>{text}</p></article>})}</div></div></section>}
